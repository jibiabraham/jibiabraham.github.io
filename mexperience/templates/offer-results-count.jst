<div class="row top-buffer loading">
    <h2 class="mk-blue">
        <% if (offersCount === 0) { %> No offers found. <% } %>
        <% if (offersCount === 1) { %> 1 offer found. <% } %>
        <% if (offersCount > 1) { %> <%= offersCount %> offers found. <% } %>
    </h2>
</div>
